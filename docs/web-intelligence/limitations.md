# Limitations

V1 provides safety architecture, offline fixtures, approval models, and optional adapter boundaries. Crawl4AI and Browser Use are not installed, interactive execution is disabled, founder-approved actions have no implementation, and no live crawl has occurred. Public sources can be wrong or conflicting and always require verification.
