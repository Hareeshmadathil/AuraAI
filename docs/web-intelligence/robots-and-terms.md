# Robots and terms

robots.txt is enforced by default and uncertainty denies access. AuraAI uses an identifiable user agent, bounded request rates, and stops at CAPTCHA, paywall, login, cookie-contract, or terms prompts. Platform terms remain a founder review responsibility.
