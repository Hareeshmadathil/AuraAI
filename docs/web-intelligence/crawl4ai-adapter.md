# Crawl4AI adapter

Pinned candidate: `crawl4ai==0.9.1` (Apache-2.0, Python ≥3.10). The adapter is optional and unavailable until installed and explicitly injected. It is restricted to approved public evidence, five pages, depth one, one browser, bounded bytes, robots enforcement, and no media or executable downloads.
