# Browser Use adapter

Pinned candidate: `browser-use==0.13.4` (MIT, Python ≥3.11). It remains optional and unavailable by default. A future founder test must use one visible browser, a disposable profile, one tab by default, screenshots around actions, an action/domain allowlist, and immediate stops for login, CAPTCHA, paywall, or terms prompts.
