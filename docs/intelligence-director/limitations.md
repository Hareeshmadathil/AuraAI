# Limitations

The subsystem is offline-first, uses synthetic demonstration data, performs no live crawling, and cannot automatically approve or execute a mission. Authority and priority scores are local heuristics, not factual certainty or reach/revenue guarantees. Founder approval is mandatory.
