# Security

Imports are bounded and strictly validated. Timestamps are timezone-aware; duplicate IDs and unsafe paths are rejected; writes are atomic; CSV formulas are neutralized. Credential-shaped data, private collection, and content execution are outside the model.
