# Security

Controls include database path containment, parameterized SQL, transactions, foreign keys, bounded imports/text, strict timestamps, duplicate IDs, exact approvals, atomic exports, CSV formula neutralization, corruption handling, and defensive copies.
