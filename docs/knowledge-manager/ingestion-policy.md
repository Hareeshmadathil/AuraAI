# Ingestion policy

Only allowlisted AuraAI artifacts with hashes, bounded text, evidence, authority, freshness, and retention metadata are accepted. Secrets, private records, raw provider payloads, full articles, stale-current claims, and unsupported exact claims are rejected.
