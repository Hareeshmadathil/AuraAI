# Versioning

Historical versions are immutable. Updates create a child version, preserve prior evidence, and explicitly change the current pointer. Rollback is another version rather than history rewriting.
